import React from "react";

const Header = () => {
  return (
    <header
      style={{
        padding: "24px",
        backgroundColor: "rebeccapurple",
        marginBottom: "30px",
      }}
    >
      Header
    </header>
  );
};

export default Header;
